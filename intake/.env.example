# Copy to .env for local dev, and set these in Vercel → Settings → Environment Variables.
# Only RESEND_API_KEY is required to actually send email.

RESEND_API_KEY=
TO_EMAIL=gerochkaka@gmail.com
FROM_EMAIL=COMBINE Intake <onboarding@resend.dev>
